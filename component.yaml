name: controller
target: controller
workspace: godon

scripts:
  - pattern: "*.py"
    path: "controller"
    settings:
      summary: "Godon Controller API"
      description: "Controller logic for managing Godon systemtender lifecycles"
      tag: "controller"  # Routes to controller worker group

flows: []