name: controller
target: f/controller
workspace: godon

scripts:
  - pattern: "*.py"
    path: "controller"
    settings:
      summary: "Godon Controller API"
      description: "Controller logic for managing Godon breeder lifecycles"
      timeout: 120